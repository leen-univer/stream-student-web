import { Homepage } from "components";
import { PublicLayout } from "layouts";

const Home = () => {
  return (
    <PublicLayout>
      <Homepage />
    </PublicLayout>
  );
};

export default Home;
